<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
</head>

<body style="margin:5%">
    <h1 class="text-center"><%= action %> Form</h1>
    <hr>
    <hr>
    <form method="POST">
        <div class="form-group">
            <label for="exampleInputEmail1">Name</label>
            <input type="text" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp"
                placeholder="Name of component" name="name" <% if (results) { %> value=<%= results[0].toJSON()['name'] %> <% } %>
                <% if (del) { %>readonly <% } %>
                required >
        </div>
        <div class="form-group">
            <label for="exampleInputEmail1">Total</label>
            <input min="0" type="number" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp"
                placeholder="total number of component" name="total" <% if (results) { %> value=<%= results[0].toJSON()['total'] %> <% } %> <% if (del) { %>readonly <% } %> required>
        </div>
        <div class="form-group">
            <label for="exampleInputEmail1">Available currently</label>
            <input type="number" min="0" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp"
                placeholder="available number of component" name="available" <% if (results) { %> value=<%= results[0].toJSON()['available'] %> <% if (del) { %>readonly <% } %> <% } %> required>
        </div>
        <div class="form-group">
            <label for="exampleInputEmail1">Price</label>
            <input type="number" min="0" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp"
                placeholder="Price per component" name="price" <% if (results) { %> value=<%= results[0].toJSON()['price'] %> <% } %> <% if (del) { %>readonly <% } %> required>
        </div>
        <button type="submit" class="btn btn-primary">Submit <% if (del) { %> to delete <% } %></button>
    </form>
</body>

</html>